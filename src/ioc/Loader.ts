import { ServerWidget } from '../server/ServerWidget';
import { ConfigWidget } from '../config/ConfigWidget';
import { FileWidget } from '../file/FileWidget';

export const SystemWidget = {
    ServerWidget, ConfigWidget, FileWidget
};